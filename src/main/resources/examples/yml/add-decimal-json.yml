actions:
  - name: "demo"
    rules:
      - name: "add"
        path: "$.accounts[0]"
        params:
          key: # this is the node defining the first supplier
            name: "staticString"
            value: "rating"
          value: # this is the node defining the second supplier
            name: "staticDecimal"
            value: 4.5
