actions:
  - name: "demo"
    rules:
      - name: "add"
        path: "$.accounts[*]"
        params:
          key: # this is the node defining the first supplier
            name: "staticString"
            value: "roleId"
          value: # this is the node defining the second supplier
            name: "staticInteger"
            value: 20
